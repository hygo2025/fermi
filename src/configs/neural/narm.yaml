model: NARM

# Model Parameters
embedding_size: 256
hidden_size: 256         # Obrigatório ser igual ao embedding_size no NARM
n_layers: 1
dropout_probs: [0.25, 0.5] # [Hidden Dropout, Attention Dropout]
loss_type: 'CE'

# Gradient Clipping
clip_grad_norm:
  max_norm: 5.0